export const HEADER_HEIGHT = 0;

export const SERVICE_FILES_GLOB = [
    '**/toc.yaml',
    '**/toc-internal.yaml',
    '**/_tocs/*.yaml',
    '**/presets.yaml',
];

export const BUILD_FOLDER = 'build';
export const BUNDLE_FOLDER = '_bundle';
export const BUNDLE_FILENAME = 'app.js';
